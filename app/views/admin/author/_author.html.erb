<tr>
  <td><%= link_to author.name, :action => 'show', :id => author %></td>
  <td><%= link_to 'Edit', :action => 'edit', :id => author %></td>
  <td><%= button_to 'Delete', admin_author_path(author),:method => :delete,
                    :confirm => "Are you sure to delete author #{author.name}?" %></td>
</tr>