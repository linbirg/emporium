<%= link_to item.book.title, catalog_path(item.book) %>
<%= pluralize(item.amount, "pc", "pcs") %>
$ <%= sprintf "%0.2f", item.price * item.amount %>
<%= button_to "-", cart_item_path(item), :method => :delete,:confirm => "Are you sure to delete this book?", :remote => true %>